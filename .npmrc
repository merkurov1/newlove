# Prefer prebuilt binaries where available (helps avoid building @swc/core)
unsafe-perm=true
engine-strict=false
fetch-retries=3
\n+# Disable optional postinstall scripts that may try to build native modules in constrained environments.
optional=false
engine-strict=false
loglevel=error
fetch-retries=2
fund=false
audit=false
optional=false
