# 🚀 SEO Аудит: merkurov.love

## Дата: 9 ноября 2025

---

## ✅ ЧТО УЖЕ РАБОТАЕТ ОТЛИЧНО

### 1. Метаданные и Open Graph

- ✅ **Качественные title/description** на всех страницах
- ✅ **Open Graph tags** настроены для social sharing
- ✅ **Twitter Cards** для расширенных превью
- ✅ **Динамические metadata** через `generateMetadata()` для статей/писем
- ✅ **Template title** для единообразия: `%s | Anton Merkurov`
- ✅ **Keywords, authors, creator** указаны в root layout
- ✅ **Canonical URLs** и language alternates настроены

### 2. Структурированные данные (JSON-LD)

- ✅ **Person schema** на главной (имя, URL, jobTitle, sameAs)
- ✅ **Website schema** с SearchAction
- ✅ **Blog schema**
- ✅ **Article schema** на страницах статей (headline, author, datePublished, publisher)
- ✅ Корректный формат (недавно исправлены проблемы с `null`)

### 3. Robots.txt и Sitemap

- ✅ **robots.ts** настроен правильно:
  - Открыт доступ ко всем публичным страницам
  - Закрыты `/admin`, `/api`, `/auth`, `/profile`
  - Отдельные правила для Googlebot
- ✅ **sitemap.ts** генерирует динамический sitemap:
  - Статические страницы
  - Статьи (до 500)
  - Проекты (до 100)
  - Письма (до 100)
  - Теги (до 200)
- ✅ **RSS feed** доступен через `/rss.xml`

### 4. Семантическая HTML разметка

- ✅ **`<article>`** для всех статей и писем
- ✅ **`<header>`** внутри article
- ✅ **`<section>`** для логических блоков
- ✅ **`<h1>`** на каждой странице (один заголовок первого уровня)
- ✅ **`<nav>`** для навигации
- ✅ **`<footer>`** с дополнительной информацией
- ✅ **aria-label** для улучшения доступности

### 5. Robots директивы

- ✅ **index: true, follow: true**
- ✅ **max-image-preview: large**
- ✅ **max-snippet: -1** (неограниченные сниппеты)

---

## 🔥 КРИТИЧЕСКИЕ УЛУЧШЕНИЯ (СДЕЛАТЬ СЕЙЧАС)

### 1. 🎯 **Добавить BreadcrumbList schema**

**Почему важно:** Google показывает хлебные крошки в результатах поиска → выше CTR

```typescript
// Добавить в app/[slug]/page.js для статей
const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    {
      '@type': 'ListItem',
      position: 1,
      name: 'Главная',
      item: 'https://merkurov.love',
    },
    {
      '@type': 'ListItem',
      position: 2,
      name: 'Статьи',
      item: 'https://merkurov.love/articles',
    },
    {
      '@type': 'ListItem',
      position: 3,
      name: article.title,
      item: `https://merkurov.love/${article.slug}`,
    },
  ],
};
```

### 2. 🖼️ **Оптимизация изображений**

**Проблема:** Изображения могут быть не оптимизированы для Core Web Vitals

**Решения:**

```typescript
// В next.config.js уже настроено remotePatterns для Supabase
// Добавить оптимизацию:
images: {
  remotePatterns: [...existing],
  deviceSizes: [640, 750, 828, 1080, 1200, 1920],
  imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  formats: ['image/webp', 'image/avif'], // Современные форматы
  minimumCacheTTL: 60 * 60 * 24 * 365, // 1 год кеш
}
```

Добавить `priority` для LCP изображений:

```jsx
<Image
  src={heroImage}
  priority={true} // ← Добавить для главного изображения
  alt="..."
  width={1200}
  height={630}
/>
```

### 3. 📱 **Добавить FAQ Schema для AI**

**Почему:** LLM (ChatGPT, Claude, Perplexity) любят FAQ-структуры

Создать `/faq` или добавить на главную:

```typescript
const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: [
    {
      '@type': 'Question',
      name: 'Чем занимается Антон Меркуров?',
      acceptedAnswer: {
        '@type': 'Answer',
        text: 'Антон Меркуров — предприниматель, художник и технолог...',
      },
    },
  ],
};
```

### 4. 🎨 **Улучшить Article Schema**

Текущая версия хороша, но можно улучшить:

```typescript
// Добавить в app/[slug]/page.js
const enhancedArticleSchema = {
  '@context': 'https://schema.org',
  '@type': 'Article', // или "BlogPosting"
  headline: content.title,
  author: {
    '@type': 'Person',
    name: content.author?.name || 'Anton Merkurov',
    url: 'https://merkurov.love',
    sameAs: ['https://twitter.com/merkurov', 'https://github.com/merkurov1'],
  },
  publisher: {
    '@type': 'Person', // или "Organization"
    name: 'Anton Merkurov',
    logo: {
      '@type': 'ImageObject',
      url: 'https://txvkqcitalfbjytmnawq.supabase.co/storage/v1/object/public/media/logo.png',
      width: 64,
      height: 64,
    },
  },
  datePublished: content.publishedAt,
  dateModified: content.updatedAt || content.publishedAt,
  image: previewImage || 'https://merkurov.love/default-og.png',
  description: description,
  mainEntityOfPage: {
    '@type': 'WebPage',
    '@id': `https://merkurov.love/${content.slug}`,
  },
  articleSection: content.tags?.[0]?.name || 'Blog', // ← Добавить категорию
  keywords: content.tags?.map((t) => t.name).join(', '), // ← Ключевые слова из тегов
  wordCount: content.content?.length || 0, // ← Количество слов
  inLanguage: 'ru-RU',
};
```

---

## 🚀 AI-ОПТИМИЗАЦИЯ (ДЛЯ LLM)

### 1. **Четкая структура контента**

✅ Уже есть: `<h1>`, `<article>`, `<header>`
🔥 Добавить:

- **Явные секции** с описательными заголовками
- **Резюме в начале** каждой статьи (для AI summary)
- **Ключевые термины** в `<strong>` или `<em>`

### 2. **Метатеги для AI**

Добавить в `app/layout.tsx`:

```typescript
export const metadata = {
  // ...existing
  other: {
    // Для Claude, ChatGPT, Perplexity
    robots: 'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1',
    google: 'notranslate', // Если контент только на русском
    'format-detection': 'telephone=no', // Отключить автодетект телефонов
  },
};
```

### 3. **Structured Summary для AI**

Добавить в начало каждой статьи:

```jsx
<div itemScope itemType="https://schema.org/Article">
  <meta itemProp="name" content={article.title} />
  <meta itemProp="author" content="Anton Merkurov" />
  <meta itemProp="datePublished" content={article.publishedAt} />
  <div itemProp="articleBody">{/* Контент статьи */}</div>
</div>
```

### 4. **AI Crawlers Whitelist**

Убедиться что robots.txt разрешает:

- `GPTBot` (OpenAI)
- `Claude-Web` (Anthropic)
- `PerplexityBot`
- `Applebot` (Apple Intelligence)

Добавить в `app/robots.ts`:

```typescript
{
  userAgent: ['GPTBot', 'Claude-Web', 'PerplexityBot', 'Applebot'],
  allow: '/',
  disallow: ['/admin', '/api', '/auth']
}
```

---

## ⚡ PERFORMANCE (CORE WEB VITALS)

### 1. **Fonts Optimization**

```typescript
// app/layout.tsx
import { Inter } from 'next/font/google';

const inter = Inter({
  subsets: ['latin', 'cyrillic'],
  display: 'swap', // ← Важно для CLS
  preload: true,
  variable: '--font-inter',
});
```

### 2. **Lazy Loading для тяжелых компонентов**

✅ Уже используется `next/dynamic` для:

- `CloseableHero`
- `AuctionSlider`
- `BentoArticlesFeed`
- `FlowFeed`
- `BackgroundShapes`

🔥 Добавить для изображений вне viewport:

```jsx
<Image
  src={image}
  loading="lazy" // ← Для изображений ниже fold
  alt="..."
/>
```

### 3. **Prefetch Critical Resources**

Добавить в `app/layout.tsx`:

```jsx
<head>
  <link rel="preconnect" href="https://txvkqcitalfbjytmnawq.supabase.co" />
  <link rel="dns-prefetch" href="https://txvkqcitalfbjytmnawq.supabase.co" />
  <link rel="preload" as="image" href="/logo.png" />
</head>
```

---

## 📊 ANALYTICS & MONITORING

### 1. **Google Search Console**

✅ Уже настроено (судя по упоминанию о проблемах с null)
🔥 Проверить:

- Core Web Vitals отчет
- Mobile Usability
- Index Coverage
- Sitemaps статус

### 2. **Добавить организационную разметку**

Если это персональный бренд/компания:

```typescript
const organizationSchema = {
  '@context': 'https://schema.org',
  '@type': 'Person', // или "Organization"
  name: 'Anton Merkurov',
  url: 'https://merkurov.love',
  logo: 'https://merkurov.love/logo.png',
  sameAs: [
    'https://twitter.com/merkurov',
    'https://github.com/merkurov1',
    'https://linkedin.com/in/merkurov',
  ],
  contactPoint: {
    '@type': 'ContactPoint',
    contactType: 'Customer Service',
    email: 'hello@merkurov.love',
  },
};
```

---

## 🎯 ПРИОРИТЕТНЫЙ ПЛАН ВНЕДРЕНИЯ

### Фаза 1: КРИТИЧНО (Сделать сегодня)

1. ✅ ~~Исправить null в structured data~~ — ГОТОВО
2. 🔥 **Добавить BreadcrumbList schema** — 30 минут
3. 🔥 **Оптимизация изображений** (priority, formats) — 1 час
4. 🔥 **AI crawlers в robots.txt** — 10 минут

### Фаза 2: ВАЖНО (На этой неделе)

1. **FAQ Schema** на главной — 2 часа
2. **Улучшить Article Schema** (keywords, wordCount) — 1 час
3. **Fonts optimization** — 30 минут
4. **Prefetch/preconnect** — 30 минут

### Фаза 3: ХОРОШО БЫ (В ближайший месяц)

1. **Microdata** для статей (itemProp)
2. **VideoObject schema** если есть видео
3. **Review schema** если есть отзывы
4. **Breadcrumbs UI** (не только schema, но и визуальные)

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Короткий срок (1-2 недели):

- ✅ Исчезновение ошибок в Google Search Console
- ✅ Улучшение CTR на 10-15% (за счет breadcrumbs)
- ✅ Быстрая индексация новых страниц

### Средний срок (1-2 месяца):

- 📈 Рост органического трафика на 20-30%
- 🤖 Частые упоминания в AI-чатах (ChatGPT, Claude)
- 🎨 Rich results в Google (статьи, FAQ, breadcrumbs)

### Долгий срок (3-6 месяцев):

- 🚀 Top-3 позиции по брендовым запросам
- 🌟 Featured snippets для FAQ
- 💎 Авторитетность домена (domain authority 40+)

---

## 🛠️ ИНСТРУМЕНТЫ ДЛЯ МОНИТОРИНГА

1. **Google Search Console** — обязательно
2. **Google PageSpeed Insights** — Core Web Vitals
3. **Schema.org Validator** — проверка JSON-LD
4. **Rich Results Test** — тестирование разметки
5. **Lighthouse** (встроен в Chrome DevTools)
6. **Screaming Frog** — полный аудит сайта

---

## ✨ БОНУС: СОВРЕМЕННЫЕ SEO ТРЕНДЫ 2025

### 1. **AI Overview Optimization**

Google показывает AI-генерируемые ответы → нужна структурированная информация:

- FAQ
- Пошаговые инструкции
- Списки
- Таблицы с данными

### 2. **E-E-A-T для AI**

Experience, Expertise, Authoritativeness, Trustworthiness:

- Указывай автора на каждой статье
- Ссылайся на авторитетные источники
- Обновляй старые статьи (dateModified)

### 3. **Voice Search**

Оптимизация под голосовой поиск:

- Естественный язык
- Вопросы и ответы
- Длинные ключевые фразы (long-tail)

### 4. **Video SEO**

Если добавишь видео:

```typescript
const videoSchema = {
  '@context': 'https://schema.org',
  '@type': 'VideoObject',
  name: 'Название видео',
  description: 'Описание',
  thumbnailUrl: 'https://...',
  uploadDate: '2025-11-09',
  duration: 'PT10M30S', // 10 минут 30 секунд
  contentUrl: 'https://...',
  embedUrl: 'https://...',
};
```

---

## 🎉 ЗАКЛЮЧЕНИЕ

**Твой сайт уже имеет отличный SEO-фундамент!** Основные улучшения:

✅ **Сделано:** Метаданные, structured data, robots, sitemap, семантическая HTML
🔥 **Критично сейчас:** BreadcrumbList, оптимизация изображений, AI crawlers
🚀 **Следующий уровень:** FAQ schema, улучшенный Article schema, microdata

**Время на реализацию критичных улучшений:** ~2-3 часа
**Ожидаемый эффект:** +20-30% органического трафика за 2 месяца

Начать с Фазы 1, затем постепенно внедрять остальное. Главное — не останавливаться, SEO это марафон, не спринт! 🏃‍♂️💨
